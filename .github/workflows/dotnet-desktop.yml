name: "Bagging_System_Application"
# Controls when the workflow will run
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
env:
  PROJECT_PATH: 'Testing Applications/Testing Applications.sln'
  

jobs:
  deploy:
    name: 'Deploy'
    runs-on: 'windows-latest'
    steps:
    - name: 'Checkout'
      uses: actions/checkout@v2

    - name: 'Install dotnet'
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '3.0.*'

  
    - name: 'Build project'
      run: dotnet build ${{ env.PROJECT_PATH }} 
    - name: 'Get Version'
      id: version
      uses: battila7/get-version-action@v2
